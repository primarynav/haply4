{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "types": ["vite/client"],

    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "skipLibCheck": true,

    // Type checking only — vite/esbuild does the emitting, and esbuild strips
    // types without checking them. Without `npm run typecheck` a broken call
    // signature ships silently.
    "noEmit": true,

    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  },
  // The live app only: main.tsx -> App.tsx -> haply/*. src/components/ is an
  // earlier UI that nothing imports any more and no longer compiles; including
  // it would bury real errors under ~140 dead-code ones.
  // scripts/ is build-time code, not shipped to the browser, but it renders the
  // legal pages from the same source the app uses — so a change to
  // legalContent.ts that breaks the generator has to fail the build, not the
  // deploy. Only the two files vite.config.ts actually imports; the .mjs
  // seeding scripts are one-offs.
  "include": ["src/main.tsx", "src/App.tsx", "src/haply", "scripts/staticPages.ts", "scripts/articles.ts"],
  "exclude": ["node_modules", "build", "supabase"]
}
